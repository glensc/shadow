sudo: false

language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - build-essential
      - automake
      - autoconf
      - autopoint
      - libtool
      - gettext
      - selinux-policy-dev
      - pkg-config

script:
 - ./autogen.sh
 - make -j4

# vim:et:ts=2:sw=2
