language: c

compiler:
 - gcc
 - clang

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq automake libtool make python zlib1g-dev

script:
 - ./autogen.sh
 - make -j4
 - make -C tests check
